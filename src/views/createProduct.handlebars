<div>
    <h1>Usuario: {{user.name}}</h1>
    <p class="userId" id="{{user.id}}">Usario Id: {{user.id}}</p>
    <p class="userEmail" id="{{user.email}}">Email: {{user.email}}</p>
    <p class="userRole" id="{{user.role}}">Rol: {{user.role}}</p>
    <a href="/api/sessions/logout"><button>cerrar sesion</button></a>
    <a href="/products"><button>Volver</button></a>       
</div>
<div>
    <h1>Crea un nuevo producto</h1>

    <form id="createProductForm">
        <label>Titulo</label>
        <input name="title">
        <br>

        <label>Descripcion</label>
        <input name="description">
        <br>

        <label>Precio</label>
        <input type="number" name="price">
        <br>

        <label>Categoria</label>
        <input name="category">
        <br>

        <label>Thumbnail</label>
        <input name="thumbnail">
        <br>

        <label>Code</label>
        <input type="number" name="code">
        <br>

        <label>Stock</label>
        <input type="number" name="stock">
        <br>

        <input type="submit">
        <br>
    </form>
    <p id="message"></p>
</div>
{{#if rolPremium}}
<script src="/js/createProduct.js"></script>
{{/if}}
{{#if rolAdmin}}
<script src="/js/createProductAdmin.js"></script>
{{/if}}